@AGENTS.md
@design_handoff_nursecomm/README.md

## API 연동 원칙

- 서버 API에 필요한 데이터가 없을 때, UI 컴포넌트를 임의로 제거하지 않는다.
- 대신 다음을 제시한다:
  1. 어떤 데이터가 부족한지
  2. 서버 API에 해당 필드를 추가하는 방안 (엔드포인트, 필드명, 타입 제안)
  3. 또는 기존에 접근 가능한 다른 API/데이터로 프론트에서 추론할 수 있는 방안
- UI 제거 여부는 사용자가 판단하고 결정한다.
